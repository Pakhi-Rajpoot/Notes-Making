<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
    <link rel="stylesheet" href="/stylesheets/profile.css">
</head>
<body>
    <div class="main">
        <div class="logoutBox">
             <a href="/logout" class="logout">Logout</a>
            </div>
       
       <h3><span>Hello, </span><%= user.name %></h3>
       
       <h4 style="margin-bottom: 20px;">you can create a new post</h4>
       <form action="/post" method="post">
        <textarea name="content" class="textarea" placeholder="what's on your mind ?"></textarea>
        <input type="submit" value="Create new post" class="btn">

       </form>
       <div class="posts">
        <h4>Your posts</h4>
       </div>

       
      
        <div class="postcontainer">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="Mypost">
                    <h4 class="someuser">@<%= user.username %></h4>
                    <p class="content"><%= post.content %></p>
                    <div class="btns">
                        <a href="/edit/<%= post._id %>" class="edit">Edit</a>
                       
                        <a href="/like/<%= post._id %>" class="like">
                            
                            <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
                        </a>
                        <small><%= post.likes.length %> Like</small>
                    </div>
                </div>
        
           <% }) %>
            
           </div>
      
       
       
    </div>
</body>
</html>